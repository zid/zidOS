OUTPUT_FORMAT("elf32-i386")
OUTPUT_ARCH("i386:x86-64")

ENTRY(start)

SECTIONS
{
	. = 0x100000;
	.text : AT(0x100000) {
		header.o (.text);
		* (.text);
	}
	.rodata : { * (.rodata); }
	.data : {
		* (.data);
	}
}
